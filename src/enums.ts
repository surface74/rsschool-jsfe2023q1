export enum Endpoint {
    EVERYTHING = 'everything',
    SOURCES = 'sources',
}

export enum HTTPMethod {
    GET = 'GET',
    POST = 'POST',
}

export enum ResponceStatus {
    'Unauthorised' = 401,
    'NotFound' = 404,
}
